"use strict"// treat all JS code a s newer version(automaticallyused internally by engine)

// alert(3+3); // we are using nodejs, not browser

let fname = "Chirayu";
let age = 27;
let isLoggedIn = false;
let state = null;//empty

//primitive-datatypes: number, String, boolean, bigInt, 
//null => standalone value
// undefined => value is not assigned
//symbol => to find uniqueness in react component

//object

console.log(typeof undefined);//undefined
console.log(typeof null);// object


